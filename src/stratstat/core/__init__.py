"""Core computation module.

Contains all metric implementations. Must never import from stratstat.report
or any visualization library (matplotlib, plotly). This boundary is enforced
by tests/test_boundaries.py.
"""
