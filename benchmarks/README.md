# Benchmarks

> 📝 **Placeholder.** Performance benchmarks comparing pure-numpy vs numba-accelerated
> paths will be added once the relevant computation paths are implemented (Phase 3+).
>
> These benchmarks provide credibility claims for StratStat's performance and guide
> users on when `stratstat[fast]` is worth the extra dependency.
