# API Overview

> 📝 **Placeholder.** This document will be populated once metrics are implemented
> (Phase 3+). It will cover:
>
> - Public API surface (`compute`, `compute_all`, `list_metrics`)
> - Input containers (`ReturnsInput`, `ExposureInput`, `TradeInput`)
> - `MetricResult` and `MetricSet` output types
> - Convention configuration (`set_default` / `get_default`)
> - Registry and custom metric authoring
> - Numba acceleration (`stratstat[fast]`)
> - Reporting module (`stratstat[report]`)
