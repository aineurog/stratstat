# Examples

> 📝 **Placeholder.** Runnable examples and notebooks will be added as metrics are
> implemented and the library becomes usable. These will live separately from `tests/`
> — tests prove correctness, examples aid adoption.
